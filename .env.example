# 旧版 compat 接口的特性开关。设置为 0 可强制插件直接调用新的 service 层。
LEGACY_COMPAT=1

# 全局维护模式开关。开启后 compat 入口会立即抛出 CompatRouteDisabled，插件需自行回退。
MAINTENANCE_MODE=0

# 灰度阶段需要禁用的 compat 入口列表，以逗号分隔。例如：
#   COMPAT_DISABLED_PATHS=legacy_user_sync,legacy_alias_lookup
COMPAT_DISABLED_PATHS=
